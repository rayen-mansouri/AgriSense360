framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        enabled: true
        stateless_token_ids:
            - submit
            - authenticate
            - logout
